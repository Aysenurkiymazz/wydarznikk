<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title><%= event.name %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

  <!-- Navbar (ortak parça) -->
  <%- include('partials/navbar') %>

  <div class="container">
    <h2><%= event.name %></h2>
    <img src="<%= event.imageUrl %>" width="300" style="border-radius: 8px; margin-bottom: 15px;" />
    
    <p><strong>Data:</strong> <%= event.date.toDateString() %></p>
    <p><strong>Lokalizacja:</strong> <%= event.location %></p>

    <h3>Uczestnicy:</h3>
    <% if (guests.length === 0) { %>
      <p>Brak uczestników.</p>
    <% } else { %>
      <ul>
        <% guests.forEach(guest => { %>
          <li><%= guest.userName %></li>
        <% }) %>
      </ul>
    <% } %>

    <form method="POST" action="/events/<%= event._id %>/dolacz">
      <button type="submit">Dołącz do wydarzenia</button>
    </form>

    <br>
    <a href="/events">← Wróć do wydarzeń</a>
  </div>

</body>
</html>


