<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edytuj wydarzenie</title>
  <link rel="stylesheet" href="/css/styles.css">
  <!-- GOOGLE PLACES API -->
  <script src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&libraries=places"></script>
</head>
<body>

  <!-- Navbar -->
  <%- include('partials/navbar') %>

  <!-- Hero başlık -->
  <div class="hero">
    <div>
      <h1>✏️ Edytuj wydarzenie</h1>
      <p>Zaktualizuj informacje o swoim wydarzeniu</p>
    </div>
  </div>

  <!-- Form -->
  <div class="container">
    <form action="/events/update/<%= event._id %>" method="POST">
      <label for="name">📛 Nazwa wydarzenia:</label>
      <input type="text" id="name" name="name" value="<%= event.name %>" required>

      <label for="date">📅 Data wydarzenia:</label>
      <input type="date" id="date" name="date" value="<%= event.date ? new Date(event.date).toISOString().substr(0, 10) : '' %>" required>

      <label for="guestList">👥 Lista gości (oddziel przecinkami):</label>
      <textarea id="guestList" name="guestList" rows="3" placeholder="jan.kowalski,anna.nowak"><%= event.guestList.join(',') %></textarea>

      <label for="location">📍 Lokalizacja:</label>
      <input
        type="text"
        id="location"
        name="location"
        value="<%= event.location || '' %>"
        required
        pattern=".{3,}"
        title="Podaj lokalizację co najmniej 3 znaki.">

      <label for="imageUrl">🖼️ Link do zdjęcia (opcjonalnie):</label>
      <input type="url" id="imageUrl" name="imageUrl" value="<%= event.imageUrl || '' %>">

      <button type="submit" class="primary-button">💾 Zapisz zmiany</button>
    </form>

    <p style="text-align: center; margin-top: 20px;">
      <a href="/events">← Powrót do wydarzeń</a>
    </p>
  </div>

  <!-- Footer -->
  <footer class="footer">
    <p>&copy; <%= new Date().getFullYear() %> Eventify. Wszystkie prawa zastrzeżone.</p>
  </footer>

  <!-- Autocomplete Script -->
  <script>
    function initAutocomplete() {
      const input = document.getElementById('location');
      const autocomplete = new google.maps.places.Autocomplete(input, {
        types: ['(cities)'],
        componentRestrictions: { country: 'pl' } // sadece Polonya
      });
    }

    document.addEventListener("DOMContentLoaded", initAutocomplete);
  </script>

</body>
</html>
